import React from 'react';
import styled from 'styled-components';

const BackgroundTop = () => <BackgroundDiv />

const BackgroundDiv = styled.div`
    background-image: radial-gradient(#5C5C5C , #000000);
    bottom: 0;
    height: 100vh;
    position: absolute;
    right: 0;
    width: 100%;
`;

export default BackgroundTop;